.DS_STORE
.idea
.stack-work
out
a.out
*.class

# Validation corpora cloned at runtime
corpora/

# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv
.envrc-local
.env.secrets

# pre-commit
.pre-commit-config.yaml

# cabal
cabal.project.local*
dist
dist-newstyle
dist-repl
# Workspace-local CABAL_DIR / cabal store (used e.g. when CABAL_DIR is
# pointed inside the worktree); anchored so a sibling project named
# similarly elsewhere isn't shadowed.
/.cabal-home/
/.cabal-store/
.ghc.environment.*
.ghci_history
.hie
.build
.log
ghcid.log

# haskell
*.hi
*.o
